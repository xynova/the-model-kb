# hardware index

Regenerated by `library-intake index refresh` after writes.

| name | title | summary | tags | run | license |
|------|-------|---------|------|-----|---------|
| edge0 | Edge0 | Open-source streaming MoE inference (MLX) that keeps expert weights on SSD and loads only the active set. | llm, open, local, moe, quantized, hardware | local | open |
