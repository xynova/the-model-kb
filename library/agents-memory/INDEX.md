# agents-memory index

Regenerated by `library-intake index refresh` after writes.

| name | title | summary | tags | run | license |
|------|-------|---------|------|-----|---------|
| code-world-model | Code World Model | Keeps an AI world consistent by storing rules and state in code (a coding agent as the “brain”), then uses a video model (MiniMax-H3 + CWM LoRA) only for what you see. | agentic, video, open, local, memory | local | open |
| voicemem | VoiceMem | Open memory layer for voice assistants: facts in one store, personality and emotion in another, built for low-latency streaming use (Apache-2.0). | memory, audio, open, local, agentic | local | open |
